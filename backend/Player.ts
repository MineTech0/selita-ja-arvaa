class Player {
    name: string
    room: string
    id: string
    constructor(name: string, room: string, id: string) {
        this.name = name
        this.room = room
        this.id = id
    }
}

export default Player